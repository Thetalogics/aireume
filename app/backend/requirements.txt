# Core dependencies
fastapi==0.141.1
starlette==1.3.1
uvicorn[standard]==0.32.1
python-multipart==0.0.31
pydantic==2.10.3
pydantic-settings==2.6.1

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10

# Authentication & Security
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
cryptography>=43.0.0
python3-saml==1.16.0

# HTTP Client
httpx==0.28.1

# Antivirus scanning (optional; only used when CLAMAV_HOST is configured)
clamd==1.0.2

# PDF Processing
pypdf==6.16.1
pdfplumber==0.11.9
pdfminer.six==20251230
python-docx==1.1.2
pymupdf==1.25.1

# Document Processing
# NOTE: LibreOffice (soffice) is a REQUIRED system dependency for .doc to PDF conversion.
# Install via: apt-get install -y libreoffice (Debian/Ubuntu) or download from libreoffice.org (Windows)
docx2txt==0.8
striprtf==0.0.26
odfpy==1.4.1
antiword==0.1.0
mammoth>=1.8.0

# NLP & Text Processing
spacy==3.8.2
presidio-analyzer==2.2.355
presidio-anonymizer==2.2.355

# Metrics & Monitoring
prometheus-client==0.21.0
prometheus-fastapi-instrumentator==8.1.0

# Scheduling
apscheduler==3.10.4

# LiveKit Cloud voice dispatch (SIP + agent dispatch from backend)
livekit-api>=0.8.0
livekit-protocol>=0.8.0

# Testing
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-timeout==2.3.1

# PDF Generation
weasyprint>=61.0

# Data Export
pandas>=2.2.0
openpyxl>=3.1.0

# LLM Integration
langchain-core>=0.3.0
langchain-ollama>=0.3.0
langchain-google-genai>=2.0.0
outlines[gemini,ollama]>=0.1.11
google-genai>=1.0.0
ollama>=0.4.0

# Utilities
python-dotenv==1.2.2
dateparser>=1.2

# OCR for scanned PDFs (optional — system must have tesseract installed)
pytesseract>=0.3.13
pdf2image>=1.17.0

# Language detection for multi-language support
langdetect>=1.0.9
langid>=1.1.6

# Shared cache / rate limits across uvicorn workers
redis>=5.0.0

# MFA
pyotp>=2.9.0

# Error reporting (optional; enabled when SENTRY_DSN is set)
sentry-sdk[fastapi]>=2.0.0
